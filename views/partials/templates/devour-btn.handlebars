<li>
  {{this.food_name}}

  {{#unless devoured}}
    <button type="button" class="btn btn-danger devour" data-id="{{id}}" data-newfood="{{devoured}}">
      DEVOUR!
    </button>
  {{/unless}}

</li>